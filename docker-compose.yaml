services:
  tarefas:
    build:
      context: . # diretório atual, por isso não passa o nome da imagem
    #ports:
    #  - "80:8000"
    command: "-e main:APP"
  notificacoes:
    build:
      context: .
    #ports:
    #  - "81:8000"
    command: "-e notificacao:APP_NOTIFICACAO"
  gateway:
    build:
      dockerfile: Dockerfile.nginx
      context: .
    ports:
      - "80:8000"